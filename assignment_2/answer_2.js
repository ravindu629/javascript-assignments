function MaximalSquare(strArr) {
  // Convert the input strings to a 2D array of numbers (matrix)
  let matrix = strArr.map((row) => row.split("").map(Number));

  for (let i = 0; i < matrix.length; i++) {
    for (let j = 0; j < matrix[i].length; j++) {
      if (matrix[i][j] === 1) {
        let size = 1;
      }
    }
  }
}
